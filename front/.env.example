# Next.js
# 浏览器统一走 Next 同源代理，确保 HttpOnly Cookie 归属于前端站点
NEXT_PUBLIC_API_BASE_URL=/api/v1
# 仅供 Next 服务端和 rewrite 访问；生产 / Docker 填写内网 API 地址
# 本地：http://localhost:8000/api/v1
# compose：http://backend:8000/api/v1（前端镜像构建 ARG，改后需 rebuild front）
API_INTERNAL_BASE_URL=http://localhost:8000/api/v1
